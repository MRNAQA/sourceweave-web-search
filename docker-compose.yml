name: web-research-studio

services:
  tester:
    image: ghcr.io/astral-sh/uv:python3.12-bookworm-slim
    working_dir: /workspace
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - .:/workspace
    environment:
      UV_LINK_MODE: copy
      UV_CACHE_DIR: /tmp/uv-cache
    depends_on:
      redis:
        condition: service_healthy
      crawl4ai:
        condition: service_healthy
      searxng:
        condition: service_healthy
    command: ["sleep", "infinity"]
    restart: unless-stopped
    networks:
      - web-research-studio-net

  redis:
    image: valkey/valkey:8.0.1-alpine
    command: "valkey-server --save 30 1 --maxclients 10000 --timeout 1800"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD-SHELL", "valkey-cli ping | grep -q PONG"]
      start_period: 5s
      interval: 10s
      timeout: 3s
      retries: 5
    restart: unless-stopped
    networks:
      - web-research-studio-net

  crawl4ai:
    image: unclecode/crawl4ai:latest
    ports:
      - "19235:11235"
    environment:
      CRAWL4AI_API_TOKEN: "dev-change-me"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:11235/health >/dev/null || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks:
      - web-research-studio-net

  searxng:
    image: searxng/searxng:latest
    ports:
      - "19080:8080"
    entrypoint:
      - /bin/sh
      - -c
      - |
        if [ -f /etc/searxng/settings.yml ]; then
          grep -q '\- json' /etc/searxng/settings.yml || sed -i '/^  formats:/a\    - json' /etc/searxng/settings.yml
        fi
        exec /usr/local/searxng/entrypoint.sh
    environment:
      SEARXNG_SECRET: "dev-change-me"
      SEARXNG_LIMITER: "false"
      SEARXNG_IMAGE_PROXY: "false"
    volumes:
      - searxng-config:/etc/searxng
      - searxng-cache:/var/cache/searxng
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/healthz || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped
    networks:
      - web-research-studio-net

volumes:
  redis-data:
  searxng-config:
  searxng-cache:

networks:
  web-research-studio-net:
    driver: bridge
